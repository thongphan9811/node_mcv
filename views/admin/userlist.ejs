<!DOCTYPE html>
<html>
<head>
<style>
table {
  border-collapse: collapse;
  width: 100%;
}

th, td {
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {background-color: #f2f2f2;}
</style>
</head>
<body>

<h2>Striped Table</h2>
<p>For zebra-striped tables, use the nth-child() selector and add a background-color to all even (or odd) table rows:</p>

<table>
  <tr>
  <th>First Name</th>
  <th>Last Name</th>
  </tr>
  
  <% userCustomer.forEach(function(user) { %>
    <tr>
      <% console.log(user) %>
      <th><%= user.username; %></th>
      <th><%= user.role; %></th>
    </tr>
  <% }); %> 
</table>
<form action=<%= url + '/admin/post'; %> method="get">
  <button type="submit">get post</button><br>
</form>

</body>
</html>
